<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" data-turbolinks="false" href="/">吟香小吃鋪</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
          <li><%= link_to("註冊", new_user_registration_path,data:{ turbolinks: "false" }) %></li>
          <li><%= link_to("登入", new_user_session_path,data:{ turbolinks: "false" }) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" data-turbolinks="false">
              您好 , <%= current_user.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to("我的文章", account_posts_path,data:{ turbolinks: "false" }) %></li>
              <li class="divider"></li>
              <li><%= link_to("帳號設定", edit_user_registration_path,data:{ turbolinks: "false" }) %></li>
              <li><%= link_to("登出", destroy_user_session_path, method: "delete",data:{ turbolinks: "false" }) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
