<%= simple_form_for(post) do |f| %>
  <div class="form-group">
    <%= f.input :content, label: '請輸入留言：', input_html: { class: "form-control"} %>
  </div>
  <%= f.submit "確認", class: "btn btn-primary", data: { disable_with: "發送中..." } %>
<% end %>
